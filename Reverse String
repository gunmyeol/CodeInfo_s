class Solution {
    public void reverseString(char[] s) {
        int start = 0;
        int end = s.length - 1;
        char swap;

        while(start <= end) {
            swap = s[start];

            s[start] = s[end];
            s[end] = swap;

            start++;
            end--;
        }

    }
}
